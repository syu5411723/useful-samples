import styled from "styled-components"

const Wrapper = styled.section`
    font-family:'Poppins', sans-serif;
    
`

export const Card = () => {
    return (
        <div>
            
        </div>
    )
}
